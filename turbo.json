{
	"$schema": "https://turbo.build/schema.json",
	"globalEnv": ["NODE_ENV"],
	"pipeline": {
		"build": {
			"outputs": ["dist/**", "build/**", ".svelte-kit/**"],
			"dependsOn": ["^build"]
		},
		"generate": {
			"dependsOn": ["^generate"]
		},
		"db:migrate:deploy": {
			"cache": false
		},
		"db:push": {
			"cache": false
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
